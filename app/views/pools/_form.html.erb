<%= form_for(@pool) do |f| %>
  <% if @pool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pool.errors.count, "error") %> prohibited this pool from being saved:</h2>

      <ul>
      <% @pool.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>Name: <%= f.text_field :name %></div>
  <div>Tournament: <%= f.select(:tournament_id, options_for_select( @tournaments, -1)) %></div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
